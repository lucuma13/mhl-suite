# =============================================================================
# mhl_suite.cli — command-line front ends
# =============================================================================
# Thin terminal entry points (argparse, progress bars, coloured output). The
# verification/sealing engines live in classicmhl/, ascmhl/, shared/, and the
# cross-dialect verify orchestrator in mhl_suite.verifyall; these modules wire
# them to a CLI. Console scripts: simple-mhl (classic) and mhlver (verify-all).
# =============================================================================
